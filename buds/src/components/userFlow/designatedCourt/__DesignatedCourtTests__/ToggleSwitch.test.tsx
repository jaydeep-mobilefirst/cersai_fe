import React from "react";
import { render, fireEvent } from "@testing-library/react";
import ToggleSwitch from "../ToggleSwitch";

describe("ToggleSwitch", () => {
    // it("renders without crashing", () => {
    //     render(<ToggleSwitch enabled={true} />);
    // });

    // it("renders with correct background color based on variant prop", () => {
    //     const { container } = render(
    //         <ToggleSwitch enabled={true} variant="table" />
    //     );
    //     const toggleSwitch = container.querySelector("button");
    //     expect(toggleSwitch).toHaveClass("bg-[#D4FDC6]");
    // });

    // it("toggles the switch when clicked", () => {
    //     const { container } = render(<ToggleSwitch enabled={true} />);
    //     const toggleSwitch = container.querySelector("button");
    //     if (toggleSwitch) {
    //         fireEvent.click(toggleSwitch);
    //         expect(toggleSwitch).toHaveAttribute("aria-checked", "false");
    //     }
    // });
    
    // it("calls the apiCall function when provided", () => {
    //     const apiCallMock = jest.fn();
    //     const { container } = render(
    //         <ToggleSwitch enabled={true} apiCall={apiCallMock} />
    //     );
    //     const toggleSwitch = container.querySelector("button");
    //     if (toggleSwitch) {
    //         fireEvent.click(toggleSwitch);
    //         expect(apiCallMock).toHaveBeenCalled();
    //     }
    // });
});
