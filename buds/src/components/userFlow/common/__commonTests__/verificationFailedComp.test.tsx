import React from 'react';
import { render } from '@testing-library/react';
import VerificationFailedComp from '../VerificationFailedComp';

test('renders VerificationFailedComp', () => {
    render(<VerificationFailedComp />);
    // Add your assertions here
});
