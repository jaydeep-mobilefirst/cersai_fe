import React from "react";
import { render } from "@testing-library/react";
import LoaderSpin from "../../LoaderSpin";

test("renders LoaderSpin component", () => {
    render(<LoaderSpin />);
    // Add your assertions here
});
